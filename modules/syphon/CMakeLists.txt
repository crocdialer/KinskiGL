if(APPLE)
  ##### Syphon
  find_library(SYPHON NAMES SYPHON)
  set(MODULE_INCLUDES "${SYPHON}/Headers" PARENT_SCOPE)
  set(MODULE_LIBRARIES ${SYPHON} PARENT_SCOPE)
  FILE(GLOB MODULE_SOURCES *.mm)
else()
  FILE(GLOB MODULE_SOURCES *.cpp)
endif(APPLE)

# common header files
FILE(GLOB MODULE_HEADERS SyphonConnector.h)

# forward variables to parent scope
set(MODULE_INCLUDE_DIRS ${MODULE_INCLUDE_DIRS} PARENT_SCOPE)
set(MODULE_HEADERS ${MODULE_HEADERS} PARENT_SCOPE)
set(MODULE_SOURCES ${MODULE_SOURCES} PARENT_SCOPE)
